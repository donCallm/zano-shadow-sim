# Патчи применяются из bash/WSL — никакой конвертации переводов строк.
patches/*.patch -text

# Трек Zano целиком исполняется в WSL: скрипты, сценарии, описи — всегда LF.
zano/** text=auto eol=lf

# Файлы кошельков — двоичные, конвертация их уничтожит.
zano/wallets/** binary
